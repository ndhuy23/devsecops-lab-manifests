apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: todo-list-demo
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
